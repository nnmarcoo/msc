[workspace]
# gui and tui are being rewritten against the new core; excluded until they are.
members = ["core"]
resolver = "2"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = 3
strip = true
